# AI Worker Environment Variables

# Server configuration
HOST=0.0.0.0
PORT=8001

# Redis connection (for job queue)
REDIS_URL=redis://localhost:6379

# PostgreSQL connection (for updating photo records)
DATABASE_URL=postgresql://postgres:password@localhost:5432/autocam

# Blur detection thresholds
# Laplacian variance threshold (lower = more blurry)
# Default: 100 (good balance)
# Adjust based on your needs:
# - 50: Very strict (marks more photos as blurry)
# - 100: Balanced
# - 150: Lenient (marks fewer photos as blurry)
BLUR_THRESHOLD=100.0

# Quality score weights
# These determine how much each factor contributes to overall quality score
BLUR_WEIGHT=0.5
EXPOSURE_WEIGHT=0.3
FACE_WEIGHT=0.2

# Logging
LOG_LEVEL=INFO

